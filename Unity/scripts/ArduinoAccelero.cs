public class ArduinoAccelero  {
	void FixedUpdate() {
		float accelerationScaler = 0.001f;
		
		// Apply acceleration forces
		//GetRigidBody().AddForce(GetArduinoData('a') * accelerationScaler);
	}
}
